import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wortmanstores.jackflash',
  appName: 'Jack Flash',
  webDir: 'build'
};

export default config;
